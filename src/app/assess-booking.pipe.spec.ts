import { AssessBookingPipe } from './assess-booking.pipe';

describe('AssessBookingPipe', () => {
  it('create an instance', () => {
    const pipe = new AssessBookingPipe();
    expect(pipe).toBeTruthy();
  });
});
